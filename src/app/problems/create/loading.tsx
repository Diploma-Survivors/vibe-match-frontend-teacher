import Loading from "@/components/ui/loading";

export default function CreateContestLoading() {
  return (
    <Loading 
      title="Đang khởi tạo form tạo cuộc thi..."
      description="Vui lòng chờ trong giây lát"
    />
  );
}