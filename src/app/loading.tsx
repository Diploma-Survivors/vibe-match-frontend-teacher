import Loading from "@/components/ui/loading";

export default function RootLoading() {
  return (
    <Loading
      title="Đang tải ứng dụng..."
      description="Vui lòng chờ trong khi chúng tôi khởi tạo hệ thống"
    />
  );
}
